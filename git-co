#!/bin/bash

commit_msg="${2}"

node ${gitco_cnf_dir}/types_menu

commit_type=`cat ${gitco_cnf_dir}/type.log`

edited_commit_msg="${commit_msg%%:*} - ${commit_type}:${commit_msg##*:}"

git commit -m "${edited_commit_msg}"